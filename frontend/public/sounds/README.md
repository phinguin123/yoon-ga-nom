`tick.wav` and `win.wav` are short, synthesized (license-free) sound effects
used by the roulette wheel — see `src/features/roulette/hooks/useRouletteSound.ts`.

Feel free to replace either file with your own audio; just keep the same
filenames (or update the `src` paths in the hook) so the app keeps finding them.
